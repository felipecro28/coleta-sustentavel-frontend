import { Link } from "react-router-dom";

export default function Button (props) {
    return (
        <div>
        <buttton>
            {props.textoBotao}            
        </buttton>
        </div>
    )
}